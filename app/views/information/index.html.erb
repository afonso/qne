<%- model_class = Information -%>
<div class="page-header">
  <h1><%=t '.title', :default => model_class.model_name.human.pluralize.titleize %></h1>
</div>
<table class="table table-striped">
  <thead>
    <tr>
      <th><%= model_class.human_attribute_name(:city) %></th>
      <th><%= model_class.human_attribute_name(:state) %></th>
      <th><%= model_class.human_attribute_name(:school) %></th>
      <th><%= model_class.human_attribute_name(:expected_finish) %></th>
      <th><%= model_class.human_attribute_name(:work_at) %></th>
      <th><%= model_class.human_attribute_name(:occupation) %></th>
      <th><%=t '.actions', :default => t("helpers.actions") %></th>
    </tr>
  </thead>
  <tbody>
    <% @information.each do |information| %>
      <tr>
        <td><%= link_to information.id, information_path(information) %></td>
        <td><%= information.user_id %></td>
        <td><%= information.city %></td>
        <td><%= information.state %></td>
        <td><%= information.school %></td>
        <td><%= information.expected_finish %></td>
        <td><%= information.work_at %></td>
        <td><%= information.occupation %></td>
        <td><%=l information.created_at %></td>
        <td>
          <%= link_to t('.edit', :default => t("helpers.links.edit")),
                      edit_information_path(information), :class => 'btn btn-default btn-xs' %>
          <%= link_to t('.destroy', :default => t("helpers.links.destroy")),
                      information_path(information),
                      :method => :delete,
                      :data => { :confirm => t('.confirm', :default => t("helpers.links.confirm", :default => 'Are you sure?')) },
                      :class => 'btn btn-xs btn-danger' %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>

<%= link_to t('.new', :default => t("helpers.links.new")),
            new_information_path,
            :class => 'btn btn-primary' %>
