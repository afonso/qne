Demanda criada com sucesso, iremos verificar, se for aprovada você receberá um email.
<%= link_to t('.back', :default => t("helpers.links.back")),
              demands_path, :class => 'btn btn-default'  %>